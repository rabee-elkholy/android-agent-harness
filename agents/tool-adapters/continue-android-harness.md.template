---
name: Android Harness
alwaysApply: true
---

# {{PRODUCT}} — Continue

Follow the repo-root `AGENTS.md` and `.agents/rules/harness-rules.md`. The rules file wins.

Use `{{PY}}` for `.agents/scripts/*`. Follow the central adaptive policy and load its selected skills. Delivery requires one matching read-only verifier result.

Device: {{DEVICE_POLICY}}

{{GIT_POLICY}}
