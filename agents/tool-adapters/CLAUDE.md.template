@AGENTS.md

# {{PRODUCT}} — Claude Code

**Source of truth:** `.agents/rules/harness-rules.md` (and `AGENTS.md`).

## Subagents

After `{{PY}} .agents/scripts/review_package.py`, launch all five in parallel (same package path). Agent files: `.claude/agents/` (generated from `.agents/subagents/*.json`).

- `bug-reviewer-agent`
- `convention-reviewer-agent`
- `security-reviewer-agent`
- `perf-anr-guardian-agent`
- `regression-impact-reviewer-agent`

Wait for `BUG_PASS` `CONVENTION_PASS` `SECURITY_PASS` `PERF_PASS` `REGRESSION_PASS`. Do not invoke `code-review-guard-agent`. Do not wait for `LGTM`.

On-demand specialists (`.claude/agents/`):
- `qa-diagnostics-agent` (Logcat crash forensics)
- `android-ui-expert-agent` (Compose & RTL)
- `test-quality-reviewer-agent` (Unit & UI test quality audits)

Device: {{DEVICE_POLICY}}

{{GIT_POLICY}}

Antigravity `hooks.json` does **not** run here. Do not assemble until those five verdicts exist.

```
{{PY}} .agents/scripts/fast_kt_lint.py
{{PY}} .agents/scripts/run_gradle_task.py {{ASSEMBLE}}
{{PY}} .agents/scripts/run_device.py install-start
```
