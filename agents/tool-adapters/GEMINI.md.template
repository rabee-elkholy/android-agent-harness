# {{PRODUCT}} — Gemini CLI / Antigravity

Follow `AGENTS.md` and `.agents/rules/harness-rules.md`. That file wins.

- Python: `{{PY}}`
- Assemble: `{{PY}} .agents/scripts/run_gradle_task.py {{ASSEMBLE}}`
- Device: {{DEVICE_POLICY}}
- {{GIT_POLICY}}
- Discovery: MUST start with `{{PY}} .agents/scripts/project_graph.py --feature <name>` or `--find <Symbol>`. Unanchored grep cascades are forbidden.
- Background tasks: Wait for reactive background completion messages; never poll or loop on `manage_task status`.
- Verification fixes: Run `{{PY}} .agents/scripts/workflow.py resume` to return to `IMPLEMENTING` when fixes are needed; record reviewer verdicts with `{{PY}} .agents/scripts/record_review.py`.
- Independent Reviews: Reviewer roles selected by policy MUST be executed via `invoke_subagent`. The lead agent is strictly forbidden from self-certifying reviews using `record_review.py --verdict PASS` on HIGH/CRITICAL or sensitive changes; evaluations must come from actual reviewer runs.
- Mobile Verification Walkthrough: After every device installation (`run_device.py install-start`), the agent MUST output a complete, numbered mobile verification walkthrough (Navigation path, Preconditions, User actions, Expected results, Edge cases) in chat BEFORE invoking `ask_question` for developer sign-off. The agent must NEVER invoke `ask_question` with just 'Did it pass' without detailing how to navigate to and test the specific modified feature.
- Antigravity Planning Lifecycle: The interactive Proceed button appears ONLY on initial task plan approval. Subsequent updates show only Review. Never tell the developer to click 'Proceed' on follow-ups or bug fixes during active tasks; execute, compile, and deploy directly.

Antigravity loads `.agents/hooks.json`; Gemini CLI does not. Detect and report the actual enforcement tier, then follow the central adaptive policy.
