# {{PRODUCT}} — Gemini CLI / Antigravity

Follow `AGENTS.md` and `.agents/rules/harness-rules.md`; the kernel wins. Python: `{{PY}}`. Device: {{DEVICE_POLICY}} {{GIT_POLICY}}

- Discover with `{{PY}} .agents/harness.py task-context --file <path> --json` (or `--symbol`) before source inspection. Use broad `project_graph.py --feature` only when needed.
- Discuss engineering questions and surface requested edge cases in chat before planning; clarify material ambiguity. Mutations require an approved plan.
- **Antigravity Planning Lifecycle**: Proceed appears only for initial approval. Active-scope follow-ups execute immediately without a new plan.
- Run classifier and central policy. Follow only selected skills, gates, reviewers, and device checks.
- Verification order: preflight → tests → independent reviewers → assemble/device → Mobile Verification Walkthrough and sign-off → read-only verify.
- Do not self-certify reviews using `record_review.py --verdict PASS`; use real reviewer output. Never poll repeatedly (**Zero-Polling**).
- Installed `.agents/scripts/**` is infrastructure. Inspect it only after command failure, contract violation, Doctor inconsistency, or an explicit developer request.
- Record context notes with `{{PY}} .agents/harness.py context note "<note>"`.

## Canonical Command Catalog

| Step | Command |
|---|---|
| Task context | `{{PY}} .agents/harness.py task-context --file <path> --json` |
| Broad discovery | `{{PY}} .agents/scripts/project_graph.py --feature <name>` |
| Task | `{{PY}} .agents/harness.py task status --task-id <id> --next` |
| Preflight | `{{PY}} .agents/harness.py preflight` |
| Tests | `{{PY}} .agents/harness.py test` |
| Resume | `{{PY}} .agents/harness.py task resume --task-id <id>` |
| Assemble | `{{PY}} .agents/harness.py assemble {{ASSEMBLE}}` |
| Device | `{{PY}} .agents/harness.py device install-start` |
| Verify | `{{PY}} .agents/harness.py verify --task-id <id>` |

Antigravity may load `.agents/hooks.json`; Gemini CLI may be rule-enforced only. Report the detected enforcement tier honestly.
