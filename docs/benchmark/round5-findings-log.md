# Round 5 findings log

| id | phase | severity | finding | status |
|---|---|---|---|---|
| F1 | install | medium | install-or-update-prompt.md hardcodes Antigravity as host; wizard marks antigravity recommended; Phase 4 text promises GEMINI.md | open |
| F2 | install | medium | Claude Code sandbox cannot write ~/.android-harness; prompt has no path for a sandboxed host (developer provisioned kit) | open |
| D1 | install | medium | discover_launchers includes androidTest manifests (TestActivity recommended as launcher) and disabled activity-aliases | open |
| D2 | install | high | install() refuses any existing `.agents` (lifecycle.py:872), and the harness owns `.agents` wholesale (install/update replace it, failure rollback and uninstall rmtree it). Repos that already use the `.agents/skills` convention cannot install without moving their files. Install prompt Phase 1 says "no existing managed `.agents`", which is looser than the code. Developer moved `.agents/skills` to `tools/agent-skills` in a commit | design-level, open |
| F3 | install | low | `.harness-setup/answers.json` and SETUP_ANSWERS.md are written before the `.agents` refusal, leaving untracked files after a refused install | open |
| F4 | install | low | In headless Claude Code the agent ran `init` with run_in_background and ended its turn; the process died after step 4 (ownership written, no test baseline). Install prompt does not say to run init in the foreground | open |
| E1 | env | n/a | Orchestrator ran baseline assemble in parallel with the installer's baseline capture; concurrent Gradle in one project broke the build cache. Not a harness defect | closed |
| P1 | install | pass | doctor --install-check 35/0/0; full doctor 55/1(ADB)/0; PreToolUse hook live-denied `baseline_capture.py` outside a task; tiers: mutation HARD_ENFORCED, plan approval RULE_ENFORCED, reviewer V2 UNVERIFIED on Claude | ok |
| F5 | install | low | After the killed init, no test baseline exists; doctor (both modes) does not report it, and `baseline_capture.py` is denied outside a task, so the developer has no documented way to re-capture it before the first task | open |
| F6 | install | low | doctor reports "Configured adapters for: Claude Code, GitHub Copilot" when only claude was selected; the app had its own pre-existing .github/copilot-instructions.md | open |
| P0 | baseline | pass | :app:assembleDebug 19m38s (cold, via Maven Central mirror init script); utils+model testDebugUnitTest green 46s | ok |
| T1-a | T1 | pass | Agent found the feature already existed in 3 paths and asked scoping questions before drafting (pre-planning rule works on Claude) | ok |
| O1 | T1 | medium | The same one-string Toast change routed 5, then 3, then 0 reviewers across three drafts; routing follows the surfaces the agent declares, so it is not stable at plan time; the diff-based reclassification after implementation corrected it to 3 and asked for re-approval | mitigated |
| D3 | T1 | critical | pre_tool_safety denied every `prepare-verification` without `--host antigravity`, so no Claude Code task could reach verification; Claude claiming `--host antigravity` was allowed. Fixed: Claude bridge sets HARNESS_HOOK_HOST=claude, engine requires `--host claude` there; Antigravity path unchanged (77d7602) | fixed |
| F7 | T1 | info | `update` is refused while a task is active (by design); developer cancelled T1, reverted the diff (saved as logs/T1-attempt1.diff), updated, restarted T1 | ok |
| D4 | T1 | high | check_strings treated `<string name=x>` and `<plurals name=x>` as duplicates (Android keeps separate namespaces); Pocket Casts baseline produced 90 errors and preflight failed on untouched files. Fixed: duplicates keyed by (tag, name) (ae5c856) | fixed |
| O2 | T1 | medium | Diff-scoped parity required the new key in all 20 locales; the agent wrote AI translations for 18 languages although Pocket Casts receives translations from GlotPress. `l10n-todo` / `tools:ignore` exist but setup never asks about an external translation workflow and the agent was not steered to them | recommendation |
| F8 | T1 | info | Second cancel/update/restart of T1 to pick up D4 (diff saved as logs/T1-attempt2.diff) | ok |
| D6 | T1 | critical | discover_android_modules matched only literal plugin ids; Pocket Casts applies plugins with `alias(libs.plugins.android.library)`, so 0 of 47 modules were discovered and every change mapped to the root module ':'. Fixed: resolve catalog aliases via gradle/*.versions.toml [plugins] (d7113d7) | fixed |
| D5 | T1 | high | Drift remediation printed `--expected-modules` with `lstrip(":")`, turning root module ':' into an empty entry that revise dropped, so prepare-verification repeated the same `module:` drift forever (agent stopped after two identical cycles). Fixed: keep module ids with leading colon (d7113d7) | fixed |
| D7 | T1 | low | `--expected-surfaces` accepted file paths; the plan stored them upper-cased as surfaces (e.g. `MODULES/SERVICES/.../STRINGS.XML`) with no validation error | open |
| T1-b | T1 | pass | Agent stopped on the drift loop instead of retrying, and refused to claim `--host antigravity` (D3); both correct behaviours | ok |
| F9 | T1 | medium | Harness Gradle gates exceed Claude Code's default 2-minute Bash timeout; Claude auto-backgrounds them and, in headless runs, the gate process dies when the turn ends (seen for `init` in F4 and for the unit-test gate in T1). Harness rules for Claude do not tell the agent to pass a long Bash timeout for gates | open |
| D8 | T1 | high | run_tests_gate.resolve_target_task targets a module only when exactly one non-app module changed; a change in :modules:services:localization + :modules:services:repositories fell back to :app:testDebugUnitTest, which passed while the changed SleepTimerHandlerTest never ran (false-green evidence). Not fixed in round 5: multi-module targeting needs per-module baselines (baseline covers :app only), and changing it could surface unbaselined failures on Antigravity projects | design, open |
| E2 | T1 | n/a | Robolectric fetched android-all from Maven Central directly (HTTP 429 in this container); PlaybackServiceErrorReporterTest failed on IOException. Environment fixed with robolectric.dependency.repo.url in the Gradle init script. Gate correctly refused to attribute the failure | closed |
| D9 | T1 | critical | Claude runs are review protocol V1 (record_review needs an `EVIDENCE pkg= cites=` footer) but installed `.claude/agents/*` reviewers must end with a HARNESS_REVIEW_RESULT_V2 block and nothing after it; no Claude review could be recorded. Reviewers correctly refused to change their contract mid-review. Fixed: V1 runs read the V2 block with the V2 parser when no footer exists (4853fa9) | fixed |
| A1 | T1 | info | Agent dispatched reviewers with plain prompts instead of the generated brief-<reviewer>.md, so two reviewers cited the delivery snapshot hash instead of the package hash; agent noticed and reported it | agent |
| V1 | T1 | value | test-quality-reviewer-agent found a real HIGH gap: the updated SleepTimerHandlerTest stub is inert (nothing asserts the Toast string), so a wrong key would pass | ok |
| A2 | T1 | info | After assemble the agent ran `device install-start` without asking the router, although the router's next action was `complete` (device verification disabled at setup) | agent |
| O3 | T1 | low | With DEVICE_VERIFICATION_MODE=disabled, `device install-start` still runs and fails as ENV-FAILURE ("HALT ... report to the developer") instead of stating that device verification is disabled and pointing to `task status --next` | open |
| P2 | T1 | pass | With D3-D6, D9 fixed: preflight, explicit module test gate, 3 reviews recorded via record_review (V1, V2 block), assemble all passed on Claude Code | ok |
| A3 | T1 | info | Agent drafted a requested copy change as task kind BUG, which made RED evidence mandatory | agent |
| D10 | T1 | high | The router asks for CAPTURE_RED only in IMPLEMENTING; the agent skipped it and prepare-verification accepted a BUG task without RED. After reviews and assemble the router said COMPLETE_TASK while the final verifier refused (missing RED): a dead end after the costly steps. The router's CAPTURE_RED command also has no module task, so on a module change it ran :app tests (D8) and found no failure. Fixed: router returns RED_EVIDENCE_MISSING before COMPLETE_TASK using the verifier rule; prepare-verification unchanged because 121 existing tests rely on it | fixed |
| T1 | T1 | pass | DELIVERED as 8c48a48 after 5 attempts (4 cancelled for harness updates). Final diff: 3 files, +6/-3; test verifies the new key and never the old one | ok |
| D11 | T2 | high | Six detected families claim modules/features/profile; drafting EXISTING_CHANGE on the existing ProfileHeader.kt failed as ambiguous, and both suggested remediations (exact target scope, NEW_FEATURE) also failed. Fixed: PRESERVE on an existing file in an ambiguous scope keeps local conventions (commit after f2ea00a) | fixed |
| T2-a | T2 | pass | Without being told, the agent planned a string resource (rules steered it away from inline UI text) | ok |
| T2-b | T2 | pass | Preflight hardcoded-string gate flagged both inline Toast texts on modified lines; the agent refused to evade it (e.g. concatenation) and asked the developer | ok |
| V2 | T2 | value | bug-reviewer caught a gesture conflict between the new avatar clickable and the header clickable; after the fix, regression-impact-reviewer caught an accessibility regression (two TalkBack focus stops with the same "Account" label). Both real, both missed by the implementing agent | ok |
| D7 | T1 | low | (update) Fixed: draft/revise reject unknown --expected-surfaces and extend declared modules with the modules of planned files (9a78618). Widening declared surfaces from file paths was tried and reverted: it changed 3 existing router tests by over-declaring code files | fixed |
| T2 | T2 | pass | DELIVERED after 2 review rounds (gesture conflict, a11y duplicate label both fixed). Cost $5.26 | ok |
| T3-a | T3 | pass | Unprompted, the agent planned version 139->140, MIGRATION_139_140 following the existing ADD COLUMN pattern, and schema export | ok |
| A4 | T3 | info | Asked to change the entity without a migration, the agent revised the plan without warning that an entity change without a version bump/migration fails Room's identity check on upgrade | agent |
| D12 | T3 | high | Adding `var colorTag: String?` to Bookmark was classified CRYPTO (T5_CRITICAL): diff-aware property types skipped FRAMEWORK_EXCLUDED_TYPES and 'String' resolved to utils/extensions/String.kt (MessageDigest). The first T1 attempt's BILLING/CRYPTO likely had the same class of cause. Fixed (classifier filters every dependency) | fixed |
| T3-b | T3 | pass | Room gate blocked the entity change: 'AppDatabase.kt: entity schema changed but version stayed 139. Increment version and add Migration(139, 140) or AutoMigration'. The agent refused to widen scope on its own; it offered FALSE_POSITIVE adjudication, which the developer rejected | ok |
| D14 | T3 | high | Toolchain identity used the first line of 'java -version', here 'Picked up JAVA_TOOL_OPTIONS: ... proxyPort=N'. After the container reconnected with a new proxy port, T3's finished verification went stale (EXTERNAL_INPUTS_CHANGED) with no code change. Fixed: skip 'Picked up ' notices | fixed |
| V3 | T3 | value | Reviewers caught the migration missing from the test helper's migration list and the missing dedicated MIGRATION_139_140 test; both fixed | ok |
| D13 | T3 | high | On hosts without trusted transcripts (Claude), HIGH-severity changes (e.g. Room) cannot complete from the agent: the verifier requires independent execution proof that only Antigravity can produce; the only path is a developer review override run from the developer's terminal. Sensitive surfaces (AUTH, BILLING, CRYPTO, SECURITY, SENSITIVE_DATA) cannot be delivered on these hosts at all. Neither docs/tool-support.md nor the router says this; the agent learned it at `complete` after ~$10 of reviews | design/doc, open |
| T3 | T3 | stopped | Not delivered: stopped at D13, then verification went stale from D14. Final diff saved (logs/T3-final.diff: Bookmark column, version 140, MIGRATION_139_140, test helper registration, migration test, 140.json). Cost ~$11 | stopped |
